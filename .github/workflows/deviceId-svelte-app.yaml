name: Device ID Svelte app

on:
  push:
    branches:
      - main
      - dev
    paths: 
      - 'identity-enabler/deviceId-mobile-app/**'
      - '!identity-enabler/deviceId-mobile-app/android/**'
  pull_request:
    branches:
      - main
      - dev
    paths: 
      - 'identity-enabler/deviceId-mobile-app/**'
      - '!identity-enabler/deviceId-mobile-app/android/**'

defaults:
  run:
    working-directory: identity-enabler/deviceId-mobile-app

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: '14'
          cache: 'npm'
          cache-dependency-path: identity-enabler/deviceId-mobile-app/package-lock.json
      - name: Update NPM
        run: npm install -g npm
      - name: Install dependencies
        run: npm install
      - name: Run ESLint
        run: npm run build:lint

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: '14'
          cache: 'npm'
          cache-dependency-path: identity-enabler/deviceId-mobile-app/package-lock.json
      - name: Update NPM
        run: npm install -g npm
      - uses: ArtiomTr/jest-coverage-report-action@v2

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: '14'
          cache: 'npm'
          cache-dependency-path: identity-enabler/deviceId-mobile-app/package-lock.json
      - name: Update NPM
        run: npm install -g npm
      - name: Install dependencies
        run: npm install
      - name: Build
        run: npm run build
      - name: Upload public directory
        uses: actions/upload-artifact@v2
        with:
          name: public
          path: identity-enabler/deviceId-mobile-app/public
